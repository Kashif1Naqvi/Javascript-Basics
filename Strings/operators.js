
// operator is so much imporatnt in programming languages for performing  arthmaticle and logical operations


function operator(a,b){
    function add(a,b){
        console.log(`${a} + ${b} = ${a+b}`)
    }
    function div(a,b){
        console.log(`${a} / ${b} = ${a/b}`)
    }
    function mul(a,b){
        console.log(`${a} * ${b} = ${a*b}`)
    }
    function sub(a,b){
        console.log(`${a} - ${b} = ${a-b}`)
    }
    function mod(a,b){
        console.log(`${a} % ${b} = ${a % b}`)
    }
    add(2,2)
    div(10,2)
    mul(3,3)
    sub(10,7)
    mod(10,5)
    console.log((a+b)*a)
}
operator(2,2)